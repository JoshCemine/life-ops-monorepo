# Backend

API and supporting services for the **Life Ops Dashboard**.

> **Status: placeholder.** This directory is scaffolded but does not yet contain
> application code. This README also ensures the `backend/` folder is tracked by
> git (which ignores empty directories).

## Planned scope

- REST/GraphQL API consumed by the `frontend` apps
- Data persistence, authentication, and background jobs

## Getting started

_To be documented once the service is set up._ Typical steps will include:

```sh
cd backend
# install dependencies
# configure environment variables
# run the dev server
```

See the [project root README](../README.md) for an overview of the monorepo.
